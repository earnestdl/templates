on: 
  workflow_call: 
    inputs:
      id:
        required: true
        type: string
      type: 
        required: true
        type: string
      state:
        required: true
        type: string

jobs:
  validate_${{inputs.type}}:
    runs-on: ubuntu-latest
    name: Validate ${{inputs.type}}
    steps:
    - name: run script
      run: echo validating pipeline

    - name: list all variables
      run: env | sort
