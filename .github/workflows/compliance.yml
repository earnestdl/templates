on: 
  workflow_call: 
    inputs:
      event:
        required: true
        type: string

jobs:
  compliance:
    runs-on: ubuntu-latest
    env:
      APPTYPE: bash
      event: ${{ inputs.event }}
    steps:
      - name: "List Variables"
        run: env | sort

      - name: Comply 
        run: | 
          echo hello from Compliance template
          echo event: $event
 