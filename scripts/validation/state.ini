[build_default]
APP_REGISTRY_URL = myregistry.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
APP_BUILD_PATH = src
APP_BUILD_CONTEXT = .
APP_BUILD_TARGET = runtime
APP_BUILD_ARGS = --build-arg=APP_NAME=${APP_NAME}

[deploy_dev_east]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_1 = mycluster1.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Development
APP_DEPLOY_ENV = dev
APPDYNAMICS_ENV = AppDynamics Dev East

[deploy_dev_west]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_2 = mycluster2.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Development
APP_DEPLOY_ENV = dev
APPDYNAMICS_ENV = AppDynamics Dev West

[deploy_qa_east]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_1 = mycluster1.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = QA
APP_DEPLOY_ENV = qa
APPDYNAMICS_ENV = AppDynamics QA East

[deploy_qa_west]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_2 = mycluster2.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = QA
APP_DEPLOY_ENV = qa
APPDYNAMICS_ENV = AppDynamics QA West

[deploy_stress_east]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_1 = mycluster1.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Stress
APP_DEPLOY_ENV = stress
APPDYNAMICS_ENV = AppDynamics Stress East

[deploy_stress_west]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_2 = mycluster2.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Stress
APP_DEPLOY_ENV = stress
APPDYNAMICS_ENV = AppDynamics Stress West

[deploy_prod_east]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_1 = mycluster1.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Prod
APP_DEPLOY_ENV = prod
APPDYNAMICS_ENV = AppDynamics Prod East

[deploy_prod_west]
APP_REGISTRY_URL = myregistry.com
APP_DEPLOY_REGIONS = ['east', 'west']
APP_RUNTIME_ARGS = -e APP_NAME=${APP_NAME} -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
OPENSHIFT_CLUSTER_2 = mycluster2.com
APP_NAME = myapp
APP_PLATFORM = edv
OPENSHIFT_PREFIX = ${APP_PLATFORM}-${APP_NAME}
OPENSHIFT_PROJECT = ${APP_PLATFORM}-${APP_NAME}-${APP_DEPLOY_ENV}
ASPNETCORE_ENVIRONMENT = Prod
APP_DEPLOY_ENV = prod
APPDYNAMICS_ENV = AppDynamics Prod West

